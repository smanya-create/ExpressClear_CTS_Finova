<?xml version="1.0" encoding="UTF-8"?>

<!--
	Created by ZK Studio
-->

<zk>
	<device-config>
		<device-type>ajax</device-type>
		<timeout-uri>/common/login.zul</timeout-uri><!-- An empty URL can cause the browser to reload the same URL -->
        <server-push-class>org.zkoss.zk.ui.impl.PollingServerPush</server-push-class>
        
	</device-config>
	<listener>
           <listener-class>com.iispl.cts.listener.UserSession</listener-class>
       </listener>
	<!-- Check every 5 seconds for dead browser connections -->
    <client-config>
        <keep-alive>false</keep-alive>
    </client-config>
</zk>
